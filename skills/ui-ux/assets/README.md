# UI/UX Assets
Place UI/UX-specific assets here (small icons, sample images, templates).
Avoid large binaries (fonts, big zip files). Use the root /assets for shared items.
## Assets
- assets/ (UI/UX-only)
- See root /assets for shared logos and brand-wide assets.
