```md # Qieman Design Skills（且慢设计技能库） 面向且慢（Qieman）设计团队的 **Skill / Spec / Asset** 共享仓库：沉淀 UI/UX、营销设计、数据图表、PPT、动效、小顾 IP、AI 工作流等规范与素材，作为可复用的“技能单元”，便于在 Cursor / Claude / Agent 工作流中按需调用。 > Repo structure inspired by the “Agent Skills” style: each domain is self-contained with `SKILL.md` + optional `references/` + optional `assets/`. --- ## Project structure ```text qieman-skills/ ├─ .claude-plugin/ # Claude/Agent plugin metadata │ └─ plugin.json ├─ skills/ # Skill domains (each folder = one domain) │ ├─ ui-ux/ # App UI/UX (patterns, components, handoff) │ │ ├─ SKILL.md │ │ ├─ references/ # Source of truth: specs, checklists, templates │ │ └─ assets/ # UI/UX-only small assets (logos/snippets) │ ├─ marketing-design/ # Marketing (KV/H5/poster/campaign visuals) │ │ ├─ SKILL.md │ │ ├─ references/ # Source of truth: rules, modules, export specs │ │ └─ assets/ # Marketing assets (library/* + fonts) │ ├─ data-visualization/ # Charts (ECharts, palettes, chart specs) │ │ ├─ SKILL.md │ │ └─ references/ │ ├─ ppt/ # PPT (structure, layout, delivery) │ │ ├─ SKILL.md │ │ └─ references/ │ ├─ motion/ # Motion (timing, transitions, export) │ │ ├─ SKILL.md │ │ └─ references/ │ ├─ xiaogu-ip/ # Xiaogu IP (character, expressions, poses) │ │ ├─ SKILL.md │ │ └─ references/ │ └─ ai-workflow/ # AI workflow (SOP, prompts, QA gates) │ ├─ SKILL.md │ └─ references/ └─ README.md # This file (entry & navigation) ``` --- ## Skill index - UI/UX → `skills/ui-ux/SKILL.md` - Marketing Design → `skills/marketing-design/SKILL.md` - Data Visualization → `skills/data-visualization/SKILL.md` - PPT → `skills/ppt/SKILL.md` - Motion → `skills/motion/SKILL.md` - Xiaogu IP → `skills/xiaogu-ip/SKILL.md` - AI Workflow → `skills/ai-workflow/SKILL.md` --- ## How to use 1. Start from the **Skill index** → open the domain `SKILL.md`. 2. For full specs / templates / checklists → go to the domain `references/`. 3. For reusable small assets → go to the domain `assets/` (PNG/SVG/JSON preferred). 4. For large files (huge images / videos / zip) → store externally and link from `references/`. --- ## Conventions ### Naming - Folders: `kebab-case` (e.g. `marketing-design`, `data-visualization`, `ui-ux`) - Files: `kebab-case` - Chinese docs: suffix `_zh` (e.g. `typography_zh.md`) - Versioning: `-vNN` (e.g. `icon-cool-coin-stack-v01.png`) ### Where things go - `SKILL.md` — domain entry: how to do, expected outputs, links. - `references/` — source of truth: long-form specs, checklists, templates, examples. - `assets/` — small reusable assets (avoid large binaries and duplicates).
